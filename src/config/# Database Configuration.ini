# Database Configuration
# This file contains environment variables for the job market scraping application.

<!-- 
# Database Connection Strings
DATABASE_URL=postgresql://username:password@localhost:5432/job_market_db
DATABASE_URL_DEV=sqlite:///data/job_market_dev.db

# API Keys
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_claude_api_key_here

# Scraping Configuration
SCRAPING_DELAY_MIN=1
SCRAPING_DELAY_MAX=3
MAX_RETRIES=3
USE_PROXY=false
PROXY_LIST=

# Application Settings
LOG_LEVEL=INFO
DEBUG_MODE=true
MAX_WORKERS=4

# Rate Limiting
REQUESTS_PER_MINUTE=30
LINKEDIN_RATE_LIMIT=10
INDEED_RATE_LIMIT=20
GLASSDOOR_RATE_LIMIT=15

# Configuration File Analysis
This configuration file defines parameters for a web scraping application targeting job listing websites:
- Database connections for production (PostgreSQL) and development (SQLite)
- API keys for AI services (OpenAI and Anthropic)
- Scraping settings with delays between requests (1-3 seconds) to avoid detection
- Application configuration including logging level and worker limits
- Rate limiting parameters to respect website limits and avoid being blocked
-->
